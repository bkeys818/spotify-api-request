{
  "name": "spotify-api-request",
  "version": "1.1.0",
  "author": {
    "name": "Ben Keys",
    "url": "https://github.com/bkeys818"
  },
  "license": "MIT",
  "main": "lib/index.js",
  "files": [
    "lib",
    "types",
    "tsconfig.json",
    "tsconfig.src.json"
  ],
  "types": "types",
  "scripts": {
    "pretest": "rimraf coverage",
    "test": "jest --coverage",
    "precoverage": "open http://localhost:8080",
    "coverage": "http-server -s -a localhost ./coverage/lcov-report",
    "predocs": "rimraf docs && mkdir docs",
    "docs": "typedoc --tsconfig tsconfig.src.json",
    "postdocs": "http-server -a -s localhost -p 5050 docs",
    "prebuild": "rimraf lib & rimraf types",
    "build": "tsc --build tsconfig.src.json",
    "prepublish": "yarn build"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "@babel/core": "^7.14.3",
    "@babel/preset-env": "^7.14.4",
    "@babel/preset-typescript": "^7.13.0",
    "@types/express": "^4.17.12",
    "@types/jest": "^26.0.23",
    "@types/node": "^15.12.1",
    "@types/node-fetch": "^2.5.10",
    "babel-jest": "^27.0.2",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "http-server": "^0.12.3",
    "jest": "^27.0.4",
    "open": "^8.2.1",
    "prettier": "^2.3.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.0.2",
    "ts-node": "^10.0.0",
    "typedoc": "^0.21.0",
    "typescript": "^4.3.4"
  }
}
